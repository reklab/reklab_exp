% Script file which executes prior to triggering cards.
% All variables from the workspace are retained in this script and returned to the workspace.
% To avoid variable conflict, you can add a sub-routine which has its own
% workspace.
% TriggerCards( objDevice ), starts the sampling by triggering the cards.


TriggerCards( objDevice );